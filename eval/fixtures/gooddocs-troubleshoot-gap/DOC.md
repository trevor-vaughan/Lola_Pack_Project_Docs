# Troubleshooting sync

## Sync hangs at "connecting"

This happens when the remote is unreachable or the port is blocked.

## "signature invalid" on pull

The downloaded metadata failed verification. This can occur after a key rotation.

## Duplicate package errors

Two sources declared the same package name.
