---
description: Use when capturing a one-line note; this command carries its own instructions.
---

# /note

A self-contained prompt-command. This fixture's module ships no skills at
all, so there is nothing here for a command to activate — the linter must
still accept it.
