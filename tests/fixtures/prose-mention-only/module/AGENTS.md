# prose-mention-only fixture

A command that mentions the module's skill in prose but never names it in a
code span. The structural linter must reject it: an incidental mention is not
an activation path.
