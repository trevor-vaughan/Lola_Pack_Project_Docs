#!/usr/bin/env bash
# The unanchored helper for the helper-no-anchor fixture: it only has to exist,
# and it must not mention the anchor phrases the linter greps SKILL.md for.
set -euo pipefail
echo "helper ran"
