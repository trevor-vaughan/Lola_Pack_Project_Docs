# Module instructions

This is a fixture module used to verify that `.taskfiles/scripts/lint-structure.sh`
rejects an explicit (`DO NOT AUTO-INVOKE.`) skill that has no matching
`module/commands/<name>.md` wrapper.
