# Module instructions

This is a fixture module used to verify that `.taskfiles/scripts/lint-structure.sh`
accepts a structurally valid module: a non-empty `AGENTS.md` plus one
well-formed skill with frontmatter and a `description` field.
