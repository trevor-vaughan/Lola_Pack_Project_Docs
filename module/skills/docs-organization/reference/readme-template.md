# <project-name>

> One-sentence description of what this is and who it's for.

## What this is

Two or three sentences. What problem does this solve? What kind of user is
this for? Don't bury the answer.

## Install

```bash
# Concrete install command. If multiple options exist, lead with the most
# common one and link to a deeper guide.
```

## Quickstart

```bash
# The smallest possible "it works" command. Reader must be able to copy
# this and get a result. No "see configuration" indirection — set the
# minimum required config inline if needed.
```

Expected output:

```text
# Show what success looks like.
```

## Where to go next

- For deeper usage: `docs/usage/` (if the project has grown into that).
- For maintainer/architecture context: `docs/dev/`.
- For decisions made along the way: `docs/dev/adr/` (or `docs/adr/`).

---

**Note for /docs-audit:** The "Install" and "Quickstart" sections must
always work as a copy-paste path for a new user. Detailed usage may move
to `docs/usage/` as the project grows, but README stays self-sufficient
for getting started.
