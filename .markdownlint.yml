default: true
MD013: false   # line length — prose wraps naturally, don't fail on it
MD033: false   # inline HTML (in case any doc uses it)
MD041: false   # first line need not be a top-level heading (badges/blockquotes)
MD060: false   # table column style — cosmetic pipe alignment, churns every table diff
